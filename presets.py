# CONSTANTS

# for VK
VERSION = 5.52

# for server
WEBDIR = "shell"
USER_DATA_FILE = "shell\\user_data.js"


def set_presets(base_id=None, mytoken=None, depth=None):
    return base_id, mytoken, depth
